import React from "react";
import * as S from "./styles";

import Logo from "../Logo";

const Header = () => {
  return (
    <S.HeaderContainer>
      <Logo />
    </S.HeaderContainer>
  );
};

export default Header;
